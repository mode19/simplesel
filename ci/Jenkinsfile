pipeline {
    agent {
        label 'ecs-agent'
    }
    stages {
        stage('Build') { 
            agent {
                ecs {
                   inheritFrom 'ecs-agent'
                   image 'imranhomedocker/myjenkins-inbound:0.1'
                }
            }
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
