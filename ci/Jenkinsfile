pipeline {
    agent {
        label 'ecs-agent'
    }
    stages {
        stage('Build') { 
            ecs {
                inheritFrom 'ecs-agent'
                image 'myjenkins-inbound'
            }
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}
